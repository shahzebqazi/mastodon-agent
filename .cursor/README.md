# `.cursor` in this repository

**Committed on purpose:** this folder holds a **single** Cursor rule so a fresh clone nudges agents toward `AGENTS.md` and `docs/AGENT_PROMPT.md`.

It is **not** a substitute for private machine config. Do not add hooks, API keys, or personal paths here.
